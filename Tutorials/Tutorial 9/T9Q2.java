/*
// Define the Organism class
class Organism {
    protected double initialSize; // Initial size of the organism
    protected double growthRate;  // Growth rate of the organism

    // Constructor to initialize instance variables
    public Organism(double initialSize, double growthRate) {
        this.initialSize = initialSize;
        this.growthRate = growthRate;
    }
}

// Define the Animal class (inherits from Organism)
class Animal extends Organism {
    private double eatingNeed; // Amount of eating needed

    // Constructor to initialize instance variables
    public Animal(double initialSize, double growthRate, double eatingNeed) {
        super(initialSize, growthRate); // Call the superclass constructor
        this.eatingNeed = eatingNeed;
    }

    // Method to display Animal instance variables
    public void display() {
        System.out.println("Initial Size: " + initialSize);
        System.out.println("Growth Rate: " + growthRate);
        System.out.println("Eating Need: " + eatingNeed);
    }
}
*/
